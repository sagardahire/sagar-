# sagar-
# initialize git git init  # add files git add . git commit -m "Initial commit"  # create repo on GitHub first, then link it: git branch -M main git remote add origin https://github.com/USERNAME/REPOSITORY_NAME.git git push -u origin main
